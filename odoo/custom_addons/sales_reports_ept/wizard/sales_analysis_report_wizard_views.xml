<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Sales Analysis Report Wizard Form View -->
    <record id="sales_analysis_report_wizard_form_view" model="ir.ui.view">
        <field name="name">sales.analysis.report.wizard.form</field>
        <field name="model">sales.analysis.report.wizard</field>
        <field name="arch" type="xml">
            <form string="Sales Analysis - Budget vs Actual">
                <group invisible="state == 'done'">
                    <group string="Report Parameters">
                        <field name="year"/>
                        <field name="report_type" widget="radio"/>
                        <field name="company_id" groups="base.group_multi_company"/>
                    </group>
                </group>
                <group invisible="state != 'done'">
                    <group string="Download Report">
                        <field name="report_file" filename="report_filename" readonly="1"/>
                        <field name="report_filename" invisible="1"/>
                    </group>
                </group>
                <field name="state" invisible="1"/>
                <footer>
                    <button name="action_generate_report" string="Generate Report" 
                            type="object" class="btn-primary" 
                            invisible="state == 'done'"/>
                    <button string="Close" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Sales Analysis Report Wizard Action -->
    <record id="sales_analysis_report_wizard_action" model="ir.actions.act_window">
        <field name="name">Sales Analysis - Budget vs Actual</field>
        <field name="res_model">sales.analysis.report.wizard</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="sales_analysis_report_wizard_form_view"/>
        <field name="target">new</field>
        <field name="context">{}</field>
    </record>
</odoo>
