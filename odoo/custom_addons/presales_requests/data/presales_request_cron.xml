<odoo>
  <record id="ir_cron_presales_request_deadline_check" model="ir.cron">
    <field name="name">Check Presales Deadlines</field>
    <field name="model_id" ref="model_presales_request"/>
    <field name="state">code</field>
    <field name="code">model.search([('deadline','&lt;', fields.Datetime.now()), ('state','not in', ['completed','rated'])]).message_post(body='SLA deadline has passed. Please take action.')</field>
    <field name="interval_number">1</field>
    <field name="interval_type">days</field>
    <field name="active">True</field>
  </record>
</odoo>