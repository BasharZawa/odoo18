<odoo>
    <record id="view_task_form" model="ir.ui.view">
        <field name="name">task.form</field>
        <field name="model">task</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="state"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_task_list" model="ir.ui.view">
        <field name="name">task.list</field>
        <field name="model">task</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="state"/>
            </list>
        </field>
    </record>

    <record id="action_task" model="ir.actions.act_window">
        <field name="name">Tasks</field>
        <field name="res_model">task</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_task" name="Tasks" parent="menu_request_root" action="action_task" sequence="20"/>
</odoo>